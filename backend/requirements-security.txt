# Security dependencies for AI Note Taker
# Install with: pip install -r requirements-security.txt

# JWT Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTPS/SSL
pyopenssl>=23.0.0
cryptography>=41.0.0

# Rate limiting
slowapi>=0.1.9

# Input validation and sanitization
bleach>=6.0.0
pydantic>=2.0.0

# Security scanning
bandit>=1.7.5
safety>=2.3.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Performance
uvloop>=0.17.0  # Unix-only (not available on Windows); omit on Windows or install uvloop-win32
