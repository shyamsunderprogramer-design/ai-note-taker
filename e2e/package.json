{
  "name": "ant-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for ANT AI Note Taker",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
